# Docker image dependencies (pinned). Installed into the emb-inversion image.
# Rebuild after changes: ssh faretra "cd ~/embedding-inversion-demo && docker build -f build/Dockerfile -t emb-inversion ."
# See requirements.txt (project root) for local dev + tooling dependencies.
torch==2.6.0
transformers==4.51.3
safetensors==0.5.3
numpy==2.2.3
pyyaml==6.0.2
datasets==3.3.1
accelerate==1.4.0
einops==0.8.0
tqdm==4.67.1
huggingface_hub==0.30.0
